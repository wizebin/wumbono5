export const COMPONENT_DOM_NAME = '_real_COMPONENT';

export const LEFT = 'left';
export const RIGHT = 'right';
export const BOTTOM = 'bottom';
export const TOP = 'top';

/**
 * Enum for cardinal directions
 * @enum {string}
 */
export const DIRECTIONS = {
  LEFT,
  RIGHT,
  BOTTOM,
  TOP,
};

/**
 * @typedef {Object} position
 * @property {Number} x
 * @property {Number} y
 */

/**
 * @typedef {Object} size
 * @property {Number} w
 * @property {Number} h
 */

/**
 * @typedef {('top'|'bottom'|'left'|'right')} direction
 */